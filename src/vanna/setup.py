from setuptools import setup, find_packages

setup(
    name='vanna',  # Replace 'vanna' with the actual package name
    version='0.1.0',
    packages=find_packages(),
    # Include other necessary metadata below
)
